services:
  mcp-magic:
    build:
      context: .
      dockerfile: mcp-server/Dockerfile
    restart: unless-stopped
    environment:
      - PORT=3333
    volumes:
      - usage-data:/app/data
    expose:
      - "3333"

volumes:
  usage-data:
